class VeryNewJava {
    // added some comments
    // by Bill
    boolean isDeveloper(String name) {
        if (name.equals("Mahdi")) {
            return true;
        }
        return false;
        // heyheyne
        // another comment is added.
    }
}